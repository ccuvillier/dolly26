
git remote add origin https://github.com/ccuvillier/dolly26.git

🟢 Mini-workflow Dolly26
1️⃣ Aller dans ton projet
cd F:\dolly26

2️⃣ Mettre à jour la branche principale
git checkout main        # se placer sur la branche principale
git pull origin main     # récupérer les dernières modifications depuis GitHub


Toujours commencer par mettre à jour main pour éviter les conflits.

3️⃣ Créer une nouvelle branche pour ta fonctionnalité
git checkout -b feature/nom-de-fonctionnalite


Exemple pour gérer l’input prénom :

git checkout -b feature/prenom-input


feature/ : convention pour les nouvelles fonctionnalités.

Tu es automatiquement basculé sur cette branche.

4️⃣ Travailler sur ta fonctionnalité

Modifie ton code, ajoute des fichiers si besoin.

Vérifie que tout fonctionne localement.

5️⃣ Ajouter les fichiers modifiés
git add .


Prépare tous les changements pour le commit.

6️⃣ Faire un commit clair
git commit -m "Ajouter gestion de l'input prénom et affichage dans H1"


Le message doit décrire ce que tu as fait.
Tu peux faire plusieurs commits si tu as plusieurs étapes importantes.

7️⃣ Pousser la branche sur GitHub
git push -u origin feature/prenom-input


-u : Git va se souvenir que feature/prenom-input correspond à la branche distante.
La prochaine fois, tu pourras juste faire git push.

8️⃣ Fusionner dans main (après validation)

Retourner sur la branche principale :

git checkout main
git pull origin main


Fusionner ta branche fonctionnalité :

git merge feature/prenom-input


Pousser main sur GitHub :

git push


Supprimer la branche locale (optionnel) :

git branch -d feature/prenom-input

9️⃣ Recommencer pour une nouvelle fonctionnalité

Toujours créer une nouvelle branche à partir de main.

Committer et pousser régulièrement pour sécuriser ton travail.

💡 Astuce : tu peux nommer tes branches selon un standard :

feature/ pour une nouvelle fonctionnalité

bugfix/ pour corriger un bug

hotfix/ pour corrections urgentes

experiment/ pour tester quelque chose sans toucher au main



-------------------------------------------------------

           GitHub (dépôt distant)
           -------------------
                    ^
                    | push / pull
                    |
             ----------------
             |      main    |
             ----------------
                    ^
                    |
                    | merge
                    |
        ---------------------------
        | feature/prenom-input    |
        ---------------------------
        | travail local sur la    |
        | fonctionnalité          |
        ---------------------------

Explications du schéma

main :

Branche principale, stable, reflète toujours la version en ligne sur GitHub.

Tu ne touches pas directement à main pour coder une nouvelle fonctionnalité.

feature/nom-fonctionnalité :

Chaque nouvelle fonctionnalité (ex. input prénom) se fait dans une branche séparée.

Tu peux créer plusieurs branches en parallèle : feature/affichage-H1, feature/form-validation, etc.

Push / Pull :

Tu envoies (push) tes commits locaux vers GitHub.

Tu récupères (pull) les changements de GitHub vers ton local si tu travailles sur plusieurs machines ou avec d’autres personnes.

Merge :

Une fois que ta branche fonctionnalité est terminée et testée, tu la fusionnes (merge) dans main.

Ensuite, tu peux pousser main sur GitHub pour mettre la version stable en ligne.

Branches supprimées :

Après le merge, tu peux supprimer ta branche locale et distante pour garder le dépôt propre.

💡 Règle simple :

Toujours créer une nouvelle branche pour chaque fonctionnalité ou correction.

Ne jamais travailler directement sur main.

Commit et push fréquents = sécurité maximale.